#if !defined(EXTRA_H)
#define EXTRA_H

#include <stdio.h>
#include <math.h>

int fact(int x);
int gcd(int x, int y);
int lntwo(int x);

// 1. fact - Take one argument and return the factorial of that argument. For
// example: 0! = 1.
// 2. lntwo - Take one argument and return the binary logarithm of that argument.
// For example: lntwo 32 = 5.
// 3. gcd - Take two arguments and return the greatest common divisor between
// the two arguments. For example: 36 gcd 24 = 12.

#endif // EXTRA_H
